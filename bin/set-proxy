#!/bin/bash
# doc: nastaví proxy podle toho zda jsme nebo nejsme na VPN

is_on_vpn=$(nmcli connection show --active | grep extvpn.o2.cz)
if [[ -z "$is_on_vpn" ]]; then  
  source $HOME/bin/set-proxy-off
else
  source $HOME/bin/set-proxy-on
fi